services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    restart: always
    env_file:
      - .env
    ports:
      - "1100:${BACKEND_PORT}"
    networks:
      - main
  # frontend:
  #   build: ./frontend/Dockerfile
  #   restart: always
  #   ports:
  #     - "1101:1101"
  #   networks:
  #     - main
networks:
  main:
    external: true
    name: sklad-db_main