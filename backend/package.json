{
  "name": "sklad_backend",
  "scripts": {
    "start": "nodemon --exec node --import=./ts-node-register.mjs src/index.ts",
    "build": "rm -rf build/* && tsc",
    "prod": "node build/index.js",
    "test": "jest --watch"
  },
  "type": "module",
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongodb": "^6.15.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.1",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2"
  }
}
